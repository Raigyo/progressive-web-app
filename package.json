{
  "name": "pwa",
  "version": "1.0.0",
  "description": "March 2021",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=3000",
    "jsonserver": "json-server -p 3001 --watch db.json",
    "pushserver": "nodemon pushServer.js",
    "nodeserver": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3",
    "live-server": "^1.2.1",
    "web-push": "^3.4.4"
  }
}
